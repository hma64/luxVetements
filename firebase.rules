rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEST RULES: completely open for development / staging only.
    // Do not use these rules in production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
